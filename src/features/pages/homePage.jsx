import Button from "../shared/components/ui/button";

export default function HomePage ( ) {
    return (
        <div className = "grid gap-6">
            <div>
                <h1 className="text 2x1 font-semibold tracking-tight">Bienvenido a ARGOS</h1>
            </div>
        </div>
    )
}